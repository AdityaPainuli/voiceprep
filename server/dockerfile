FROM node:22.12-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# 👇 Provide DATABASE_URL only for prisma generate
ENV DATABASE_URL="postgresql://user:pass@localhost:5432/dummy"

RUN npx prisma generate

EXPOSE 8080
CMD ["npm", "run", "start"]
